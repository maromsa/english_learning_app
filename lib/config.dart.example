// This file is kept for reference only.
//
// Runtime secrets are no longer read from `lib/config.dart`. Instead, they are
// supplied at build/run time using `--dart-define` and consumed via the
// `AppConfig` helper. For example:
//
// flutter run \
//   --dart-define=GEMINI_API_KEY=xxx \
//   --dart-define=PIXABAY_API_KEY=yyy \
//   --dart-define=CLOUDINARY_CLOUD_NAME=zzz
//
// See the README for the full list of supported keys.