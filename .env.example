# Optional override when your Firebase function is fronted by a custom domain.
GEMINI_PROXY_URL=https://your-cloud-function-url/geminiProxy
# Optional: override image validation endpoint (defaults to GEMINI_PROXY_URL when set).
AI_IMAGE_VALIDATION_URL=
